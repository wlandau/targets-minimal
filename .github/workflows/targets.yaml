on:
  push:
    branches:
      - main

name: targets

jobs:
  targets:
    runs-on: macOS-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUBPAT }}
    steps:
      - uses: actions/checkout@v2
      - uses: r-lib/actions/setup-r@master
      - uses: r-lib/actions/setup-pandoc@master

      - name: Identify R packages
        run: |
          install.packages("remotes")
          saveRDS(remotes::dev_package_deps(dependencies = TRUE), ".github/depends.Rds", version = 2)
          writeLines(sprintf("R-%i.%i", getRversion()$major, getRversion()$minor), ".github/R-version")
        shell: Rscript {0}

      - name: Cache R packages
        uses: actions/cache@v2
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-${{ hashFiles('.github/R-version') }}-custom_suffix-${{ hashFiles('.github/depends.Rds') }}
          restore-keys: ${{ runner.os }}-${{ hashFiles('.github/R-version') }}-custom_suffix-

      - name: Install R packages
        run: remotes::install_deps(dependencies = TRUE)
        shell: Rscript {0}

      - name: Cache targets data store
        uses: actions/cache@v2
        with:
          # List _targets/ and any custom output files below.
          # Delete index.html if the pipeline does not render this report.
          path: |
            _targets/
            index.html
          key: ${{ runner.os }}-targets-${{ hashFiles('_targets/meta/meta') }}
          restore-keys: ${{ runner.os }}-targets-

      - name: Run targets pipeline
        run: |
          digest::digest("index.Rmd", algo = "xxhash32", file = TRUE)
          digest::digest("index.html", algo = "xxhash32", file = TRUE)
          targets::tar_make()
          digest::digest("index.Rmd", algo = "xxhash32", file = TRUE)
          digest::digest("index.html", algo = "xxhash32", file = TRUE)
        shell: Rscript {0}

      - name: Do previous runs exist?
        id: runs-exist
        run: git fetch origin targets-runs
        continue-on-error: true

      - name: Upload first run
        if: steps.runs-exist.outcome != 'success'
        run: |
          git checkout --orphan targets-runs
          rm .gitignore
          git add *
          git commit -am "First run"
          git push -u origin targets-runs

      - name: Checkout previous run
        if: steps.runs-exist.outcome == 'success'
        uses: actions/checkout@v2
        with:
          ref: targets-runs
          fetch-depth: 1
          path: targets-runs

      - name: Upload subsequent run
        if: steps.runs-exist.outcome == 'success'
        run: |
          rm -r .git
          mv targets-runs/.git .
          rm -r targets-runs
          rm .gitignore
          git add *
          git commit -am "Run pipeline"
          git push origin targets-runs
